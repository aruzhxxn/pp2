# Practice 3 (Python)

This folder contains short examples for:
- Functions (basics, arguments, return values, *args/**kwargs)
- Lambda expressions (with map, filter, sorted)
- Classes (definition, __init__, class methods, class variables)
- Inheritance (basics, super(), overriding, multiple inheritance)

## Run
Use Python 3.

Example:
```bash
python functions/basic_functions.py 